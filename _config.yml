# Jekyll config file

baseurl: ""  # the subpath of your site, e.g. /blog
url: "https://sadraddini.github.io"  # the base hostname & protocol for your site

markdown: kramdown

theme: minima

defaults:
  - scope:
      path: ""
    values:
      layout: default
